# Async Pipeline

Asynchronous workflow pipeline with built-in graph editor.

## Installation

### 1. Start containers
```bash
docker-compose up -d --build
```

### 2. Install PHP dependencies
```bash
docker/composer install
```

## Usage

Open graph editor at [http://localhost:20045](http://localhost:20045) and create your workflow.